<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark mode theme -->
    <style name="AppTheme.Dark" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/dark_bg</item>
        <item name="android:textColorPrimary">@color/white</item>
    </style>
    <!-- Day mode theme -->
    <style name="AppTheme.Day" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/day_bg</item>
        <item name="android:textColorPrimary">@color/black</item>
        <!-- Customize other attributes as needed -->
    </style>
</resources>